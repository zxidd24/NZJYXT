# 农资现货交易系统开发手册 v2（H5门户 + PC后台）

> **核心原则**：功能优先，界面极简。
> **目标**：从零开始，按顺序完成一套可运行的农资交易系统，覆盖《农资现货交易系统功能清单》全部功能点。
> **技术栈（已定稿）**：
>
> - **后端**：Spring Boot 3.2.x + MyBatis-Plus + MySQL 8 + Redis 7 + JWT（jjwt）+ Knife4j 4.x
> - **PC管理后台**：Vue3 + Element Plus + ECharts（统计图表）+ Pinia + Vue Router
> - **H5门户（移动端）**：Vue3 + Vant 4（**按需引入**，配 `unplugin-vue-components` 自动注册）
>
> **v2 修订说明**（相对 v1 的主要变化）：
>
> 1. 补齐功能清单漏项：收货地址、发票（信息维护+开票申请）、修改密码、个人中心基本信息、用户评级定级、我的已办、电子凭证/盖章回传、贷款全流程、消息记录与通知中心、再次下单、查看发货单、"我的发布"等。
> 2. 与清单对齐：审核流程改为**可配置**（清单备注"最好是活的"）；订单审核按**销售+主管两级**设计；实名认证材料补银行卡；支付去掉"微信"，按**中行结算/钱包**模式设计，退款**原路退回**。
> 3. 技术修正：取消订单回补库存 + 待付款超时自动取消；下单/支付/退款幂等设计；退出登录真正失效（拦截器查Redis）；统一 `audit_record` 审核记录表支撑"我的已办"；新增 `agri-service` 业务模块；敏感字段（身份证/银行卡）加密存储+展示脱敏；配置中不再出现明文密码。
> 4. 工程修正：工期调整为约 25 人日；补齐初始化 SQL；补充统一规范（错误码/分页/幂等/安全）与测试计划；新增"功能清单 → 手册章节"覆盖对照表（附录A）。

---

## 一、开发环境准备

### 1.1 安装软件
- **JDK 17**（Spring Boot 3.x 的最低要求）
- **Node.js 18+**（前端运行环境）
- **MySQL 8.0+**（数据库）
- **Redis 7.0+**（缓存/会话/幂等/延迟取消）
- **IDEA**（后端IDE） + **VSCode**（前端IDE）
- **Git**（版本管理）

### 1.2 项目目录结构规划（建议物理隔离）

```
agri-trading-system/
├── backend/                      # 后端根目录（Maven多模块）
│   ├── agri-common/              # 公共模块（工具类、统一返回、异常、常量、枚举）
│   ├── agri-service/             # 业务模块（Service + Mapper，被两个API模块共同依赖，避免重复实现）
│   ├── agri-admin-api/           # PC管理后台接口（端口 8080）
│   ├── agri-portal-api/          # H5门户接口（端口 8081）
│   └── pom.xml                   # 父POM
├── frontend-admin/               # PC管理前端（Vue3 + Element Plus + ECharts，端口 3000）
└── frontend-portal/              # H5移动端前端（Vue3 + Vant 4，端口 3001）
```

> **实际开发顺序**：先完成后端接口（通过Knife4j/Postman调试），再开发前端页面。本手册以**后端接口**为主线，前端页面作为"界面配套"穿插其中。

---

## 二、统一规范（开工前必读）

### 2.1 统一返回与错误码

所有接口返回 `Result<T>`：

```json
{ "code": 0, "message": "success", "data": {} }
```

| 错误码段 | 含义 |
| :--- | :--- |
| 0 | 成功 |
| 40100~40199 | 认证失败（未登录、Token过期） |
| 40300~40399 | 无权限 |
| 10001~19999 | 参数/校验错误 |
| 20001~29999 | 业务错误（如 20101 库存不足、20102 订单状态不允许该操作、20103 重复提交） |
| 50000~59999 | 系统错误 |

> 全部业务错误码登记在 `agri-common` 的 `ErrorCodeEnum`，前后端以该枚举为唯一口径。

### 2.2 分页规范

请求参数：`pageNum`（默认1）、`pageSize`（默认10，最大100）。
返回结构：`PageResult<T>{ total, pageNum, pageSize, list }`。

### 2.3 幂等规范

- **下单**：先调 `GET /api/portal/order/token` 领取一次性幂等Token（Redis `SETNX order:token:{token}`，有效期10分钟），下单时携带，服务端校验后即删除；防重复点击与重复提交。
- **支付回调/模拟支付**：以 `order_no` 幂等，重复请求返回首次结果。
- **退款申请**：同一订单同一时刻只允许一笔进行中的退款（业务层校验：每单仅允许一条"待审核"记录，`refund_apply` 唯一键保证退款单号不重复）。

### 2.4 安全规范

- 密码：`BCryptPasswordEncoder` 加密存储，任何接口不得返回密码字段。
- 敏感字段：`id_card`、`bank_card`、`bank_account` 等使用 AES 加密入库（密钥放环境变量 `AGRI_AES_KEY`，**禁止写进代码和配置文件**），展示时脱敏（如 `6217********1234`）。
- 配置文件：数据库密码、Redis密码、JWT密钥一律使用环境变量占位符 `${DB_PASSWORD}`，**手册与代码库中不得出现真实口令**。
- 文件上传：校验扩展名白名单（jpg/png/pdf）与大小（≤5MB），重命名为 UUID 存储，防止路径穿越。

### 2.5 命名与状态口径

- 表名、字段名：`snake_case`；接口：REST 风格；Java 实体：`CamelCase`。
- **所有状态枚举集中在 `agri-common` 的枚举类维护**，与附录B《状态字典》一一对应，建表注释、后端枚举、前端字典三方必须同步修改。

---

## 三、数据库设计（核心表结构 + 初始化SQL）

> 建议新建数据库 `agri_trading`，字符集 `utf8mb4`。共 28 张表。

```sql
-- 1. 用户表（整合后台管理员和门户用户）
CREATE TABLE `sys_user` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL COMMENT '登录账号（门户用户为手机号）',
  `password` varchar(100) NOT NULL COMMENT 'BCrypt加密密码',
  `real_name` varchar(50) DEFAULT NULL COMMENT '真实姓名',
  `user_type` tinyint(4) DEFAULT NULL COMMENT '1-自然人 2-法人 3-后台管理员',
  `phone` varchar(20) DEFAULT NULL,
  `email` varchar(100) DEFAULT NULL,
  `status` tinyint(4) DEFAULT '1' COMMENT '0-禁用 1-启用',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
);

-- 2. 角色表
CREATE TABLE `sys_role` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `role_name` varchar(50) NOT NULL,
  `description` varchar(200) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
);

-- 3. 用户角色关联表
CREATE TABLE `sys_user_role` (
  `user_id` bigint(20) NOT NULL,
  `role_id` bigint(20) NOT NULL,
  PRIMARY KEY (`user_id`,`role_id`)
);

-- 4. 权限表（菜单/按钮）
CREATE TABLE `sys_permission` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `parent_id` bigint(20) DEFAULT '0',
  `name` varchar(50) DEFAULT NULL,
  `type` tinyint(4) DEFAULT NULL COMMENT '1-菜单 2-按钮',
  `perms` varchar(100) DEFAULT NULL COMMENT '权限标识，如 admin:product:add',
  `url` varchar(200) DEFAULT NULL,
  `sort` int(11) DEFAULT '0',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
);

-- 5. 角色权限关联表
CREATE TABLE `sys_role_permission` (
  `role_id` bigint(20) NOT NULL,
  `permission_id` bigint(20) NOT NULL,
  PRIMARY KEY (`role_id`,`permission_id`)
);

-- 6. 门户用户扩展信息（实名认证）
-- 注：id_card / bank_card 为AES加密密文，展示层统一脱敏
CREATE TABLE `portal_user_info` (
  `user_id` bigint(20) NOT NULL COMMENT '关联sys_user.id',
  `company_name` varchar(100) DEFAULT NULL COMMENT '法人用户的企业名称',
  `contact_name` varchar(50) DEFAULT NULL COMMENT '联系人姓名',
  `id_card` varchar(255) DEFAULT NULL COMMENT '身份证号（AES加密）',
  `business_license` varchar(100) DEFAULT NULL COMMENT '营业执照号（统一社会信用代码）',
  `business_license_img` varchar(255) DEFAULT NULL COMMENT '营业执照图片URL',
  `id_card_front` varchar(255) DEFAULT NULL COMMENT '身份证正面URL',
  `id_card_back` varchar(255) DEFAULT NULL COMMENT '身份证反面URL',
  `bank_card` varchar(255) DEFAULT NULL COMMENT '银行卡号（AES加密）',
  `bank_name` varchar(50) DEFAULT NULL COMMENT '开户行',
  `credit_grade` varchar(20) DEFAULT NULL COMMENT '商家等级/信用等级 A/B/C（管理员评级定级）',
  `credit_limit` decimal(18,2) DEFAULT '0.00' COMMENT '授信额度（随等级变化）',
  `auth_status` tinyint(4) DEFAULT '0' COMMENT '0-未认证 1-审核中 2-已认证 3-驳回',
  `auth_submit_time` datetime DEFAULT NULL,
  `auth_audit_time` datetime DEFAULT NULL,
  `auth_audit_remark` varchar(255) DEFAULT NULL,
  PRIMARY KEY (`user_id`)
);

-- 7. 收货地址表（个人中心-账户管理-收货地址，支持多个）
CREATE TABLE `user_address` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) NOT NULL,
  `receiver_name` varchar(50) NOT NULL COMMENT '收货人',
  `receiver_phone` varchar(20) NOT NULL,
  `province` varchar(20) DEFAULT NULL,
  `city` varchar(20) DEFAULT NULL,
  `district` varchar(20) DEFAULT NULL,
  `detail_address` varchar(200) NOT NULL COMMENT '详细地址',
  `is_default` tinyint(4) DEFAULT '0' COMMENT '1-默认地址',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_user` (`user_id`)
);

-- 8. 商品分类表（多级：支持二级、三级子分类，清单备注"需要子类别，可能也需要子类别的子类别"）
CREATE TABLE `product_category` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `parent_id` bigint(20) DEFAULT '0' COMMENT '0为顶级',
  `name` varchar(50) NOT NULL,
  `sort` int(11) DEFAULT '0',
  `status` tinyint(4) DEFAULT '1' COMMENT '0-停用 1-启用',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_parent` (`parent_id`)
);

-- 9. 商品表
CREATE TABLE `product` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `category_id` bigint(20) DEFAULT NULL,
  `name` varchar(200) NOT NULL,
  `sub_title` varchar(300) DEFAULT NULL,
  `brand` varchar(50) DEFAULT NULL,
  `price` decimal(18,2) NOT NULL COMMENT '单价',
  `stock` int(11) NOT NULL COMMENT '库存',
  `unit` varchar(10) DEFAULT NULL COMMENT '单位（吨/袋/瓶）',
  `description` text,
  `images` varchar(1000) DEFAULT NULL COMMENT '逗号分隔图片URL',
  `status` tinyint(4) DEFAULT '0' COMMENT '0-待审核 1-已上架 2-已下架 3-审核驳回',
  `is_recommend` tinyint(4) DEFAULT '0' COMMENT '推荐位 0-否 1-是',
  `sort` int(11) DEFAULT '0' COMMENT '展示位置排序值',
  `created_by` bigint(20) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_category` (`category_id`),
  KEY `idx_status` (`status`)
);

-- 10. 购物车表
CREATE TABLE `shopping_cart` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) NOT NULL,
  `product_id` bigint(20) NOT NULL,
  `quantity` int(11) NOT NULL,
  `selected` tinyint(4) DEFAULT '1',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_product` (`user_id`,`product_id`)
);

-- 11. 订单主表
CREATE TABLE `order_main` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `order_no` varchar(32) NOT NULL COMMENT '订单号',
  `user_id` bigint(20) NOT NULL,
  `total_amount` decimal(18,2) DEFAULT NULL COMMENT '商品总额',
  `pay_amount` decimal(18,2) DEFAULT NULL COMMENT '应付金额',
  `freight` decimal(10,2) DEFAULT '0.00' COMMENT '运费',
  `order_status` tinyint(4) DEFAULT '0' COMMENT '0-待付款 1-待审核 2-待发货 3-待收货 4-已完成 5-已取消 6-退款中 7-已退款',
  `pay_status` tinyint(4) DEFAULT '0' COMMENT '0-未支付 1-已支付',
  `pay_time` datetime DEFAULT NULL,
  `pay_method` varchar(20) DEFAULT NULL COMMENT '钱包/银行转账/额度（贷款受托支付，预留）',
  `address_id` bigint(20) DEFAULT NULL COMMENT '下单所选地址ID',
  `receiver_name` varchar(50) DEFAULT NULL COMMENT '收货人快照',
  `receiver_phone` varchar(20) DEFAULT NULL,
  `receiver_address` varchar(300) DEFAULT NULL COMMENT '完整收货地址快照',
  `audit_node_id` bigint(20) DEFAULT NULL COMMENT '当前审核节点（两级审核流转用）',
  `cancel_time` datetime DEFAULT NULL,
  `buyer_note` varchar(255) DEFAULT NULL,
  `seller_note` varchar(255) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_user_status` (`user_id`,`order_status`)
);

-- 12. 订单明细表（含分类快照，支撑"按商品分类统计/展示订单"）
CREATE TABLE `order_detail` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `order_id` bigint(20) DEFAULT NULL,
  `product_id` bigint(20) DEFAULT NULL,
  `product_name` varchar(200) DEFAULT NULL COMMENT '商品名快照',
  `category_id` bigint(20) DEFAULT NULL COMMENT '商品分类快照',
  `category_name` varchar(50) DEFAULT NULL COMMENT '分类名快照',
  `product_price` decimal(18,2) DEFAULT NULL COMMENT '成交单价快照',
  `unit` varchar(10) DEFAULT NULL COMMENT '单位快照',
  `quantity` int(11) DEFAULT NULL,
  `total_price` decimal(18,2) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_order` (`order_id`),
  KEY `idx_category` (`category_id`)
);

-- 13. 订单附件表（客户盖章回传单、加盖签章的发货单）
CREATE TABLE `order_attachment` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `order_id` bigint(20) NOT NULL,
  `type` tinyint(4) NOT NULL COMMENT '1-客户盖章回传单 2-签章发货单',
  `file_url` varchar(255) NOT NULL,
  `file_name` varchar(100) DEFAULT NULL,
  `uploaded_by` bigint(20) DEFAULT NULL COMMENT '上传人（门户用户或后台销售）',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_order` (`order_id`)
);

-- 14. 退款申请表
CREATE TABLE `refund_apply` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `refund_no` varchar(32) NOT NULL COMMENT '退款单号',
  `order_id` bigint(20) DEFAULT NULL,
  `user_id` bigint(20) DEFAULT NULL,
  `order_amount` decimal(18,2) DEFAULT NULL COMMENT '原交易金额（冗余，后台展示用）',
  `refund_amount` decimal(18,2) DEFAULT NULL COMMENT '退款金额',
  `reason` varchar(255) DEFAULT NULL COMMENT '退款原因',
  `status` tinyint(4) DEFAULT '0' COMMENT '0-待审核 1-同意 2-驳回 3-已退款',
  `refund_channel` varchar(20) DEFAULT NULL COMMENT '退款路径：钱包/银行转账（原路退回）',
  `prev_status` tinyint(4) DEFAULT NULL COMMENT '发起退款前订单状态快照（驳回时恢复用）',
  `apply_time` datetime DEFAULT NULL,
  `audit_time` datetime DEFAULT NULL,
  `auditor_id` bigint(20) DEFAULT NULL,
  `remark` varchar(255) DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_refund_no` (`refund_no`),
  KEY `idx_order` (`order_id`)
);

-- 15. 评价表
CREATE TABLE `order_comment` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `order_id` bigint(20) DEFAULT NULL,
  `user_id` bigint(20) DEFAULT NULL,
  `product_id` bigint(20) DEFAULT NULL,
  `score` int(11) DEFAULT NULL COMMENT '1-5分',
  `content` varchar(500) DEFAULT NULL,
  `images` varchar(1000) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_product` (`product_id`),
  KEY `idx_order` (`order_id`)
);

-- 16. 钱包账户（虚拟钱包）
CREATE TABLE `wallet_account` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) NOT NULL,
  `balance` decimal(18,2) DEFAULT '0.00' COMMENT '可用余额',
  `frozen_amount` decimal(18,2) DEFAULT '0.00' COMMENT '冻结金额',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_id` (`user_id`)
);

-- 17. 资金流水表（含出金/入金记录）
CREATE TABLE `wallet_transaction` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) DEFAULT NULL,
  `order_id` bigint(20) DEFAULT NULL,
  `trans_no` varchar(32) NOT NULL COMMENT '流水号',
  `amount` decimal(18,2) DEFAULT NULL COMMENT '发生额（正数）',
  `direction` tinyint(4) DEFAULT NULL COMMENT '1-入账 2-出账（汇总统计用）',
  `trans_type` tinyint(4) DEFAULT NULL COMMENT '1-入金 2-出金 3-支付 4-退款',
  `trans_status` tinyint(4) DEFAULT '0' COMMENT '0-处理中 1-成功 2-失败',
  `balance_after` decimal(18,2) DEFAULT NULL COMMENT '交易后余额',
  `remark` varchar(255) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_trans_no` (`trans_no`),
  KEY `idx_user` (`user_id`)
);

-- 18. 贷款记录表（银行授信资金只能受托支付到平台运营方结算账户，贷款不进用户钱包，系统仅记录额度）
CREATE TABLE `loan_record` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) DEFAULT NULL,
  `loan_no` varchar(32) NOT NULL COMMENT '贷款申请编号',
  `amount` decimal(18,2) DEFAULT NULL COMMENT '贷款金额',
  `credit_limit_used` decimal(18,2) DEFAULT NULL COMMENT '占用授信额度',
  `status` tinyint(4) DEFAULT NULL COMMENT '0-申请中 1-已放款 2-已还款 3-驳回',
  `apply_time` datetime DEFAULT NULL,
  `audit_time` datetime DEFAULT NULL,
  `auditor_id` bigint(20) DEFAULT NULL,
  `audit_remark` varchar(255) DEFAULT NULL,
  `release_time` datetime DEFAULT NULL COMMENT '放款时间（银行受托支付至平台结算账户）',
  `repay_time` datetime DEFAULT NULL COMMENT '还款时间（还款后授信额度恢复）',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_loan_no` (`loan_no`),
  KEY `idx_user` (`user_id`)
);

-- 19. 发票抬头信息表（个人中心-账户管理-发票信息）
CREATE TABLE `invoice_info` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) NOT NULL,
  `title_type` tinyint(4) DEFAULT '2' COMMENT '1-个人 2-企业',
  `title` varchar(100) NOT NULL COMMENT '发票抬头',
  `tax_no` varchar(50) DEFAULT NULL COMMENT '纳税人识别号',
  `bank_name` varchar(50) DEFAULT NULL COMMENT '开户银行',
  `bank_account` varchar(255) DEFAULT NULL COMMENT '银行账号（AES加密）',
  `reg_address` varchar(200) DEFAULT NULL COMMENT '注册地址',
  `phone` varchar(20) DEFAULT NULL COMMENT '注册电话',
  `is_default` tinyint(4) DEFAULT '0',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_user` (`user_id`)
);

-- 20. 开票申请表
CREATE TABLE `invoice_apply` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `apply_no` varchar(32) NOT NULL COMMENT '开票申请编号',
  `user_id` bigint(20) NOT NULL,
  `order_id` bigint(20) NOT NULL,
  `invoice_info_id` bigint(20) NOT NULL,
  `amount` decimal(18,2) NOT NULL COMMENT '开票金额',
  `status` tinyint(4) DEFAULT '0' COMMENT '0-待开票 1-已开票 2-驳回',
  `apply_time` datetime DEFAULT NULL,
  `issue_time` datetime DEFAULT NULL,
  `issuer_id` bigint(20) DEFAULT NULL COMMENT '开票经办人',
  `invoice_no` varchar(50) DEFAULT NULL COMMENT '发票号码',
  `remark` varchar(255) DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_apply_no` (`apply_no`),
  KEY `idx_order` (`order_id`),
  KEY `idx_user` (`user_id`)
);

-- 21. 文章资讯表
CREATE TABLE `article` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `title` varchar(200) DEFAULT NULL,
  `content` text,
  `category_id` bigint(20) DEFAULT NULL COMMENT '栏目',
  `author` varchar(50) DEFAULT NULL,
  `author_id` bigint(20) DEFAULT NULL COMMENT '发布人（支撑"我的发布"）',
  `source` varchar(100) DEFAULT NULL,
  `is_published` tinyint(4) DEFAULT '0' COMMENT '0-草稿 1-已发布',
  `publish_time` datetime DEFAULT NULL,
  `sort` int(11) DEFAULT '0',
  `view_count` int(11) DEFAULT '0',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_category` (`category_id`),
  KEY `idx_author` (`author_id`)
);

-- 22. 文章栏目表
CREATE TABLE `article_category` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(50) DEFAULT NULL,
  `sort` int(11) DEFAULT '0',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
);

-- 23. 消息配置表（消息提醒配置）
CREATE TABLE `message_config` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `code` varchar(50) NOT NULL COMMENT '消息编码，如 AUTH_RESULT',
  `title` varchar(100) DEFAULT NULL,
  `content_template` varchar(500) DEFAULT NULL COMMENT '模板，支持{变量}占位',
  `enabled` tinyint(4) DEFAULT '1',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code` (`code`)
);

-- 24. 消息记录表（站内消息：待办提醒、审核结果、订单状态变更等）
CREATE TABLE `message_record` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) NOT NULL COMMENT '接收人（门户用户或后台用户）',
  `config_code` varchar(50) DEFAULT NULL COMMENT '对应message_config.code',
  `title` varchar(100) NOT NULL,
  `content` varchar(500) NOT NULL,
  `biz_type` tinyint(4) DEFAULT NULL COMMENT '关联业务类型（同audit_record.biz_type）',
  `biz_id` bigint(20) DEFAULT NULL COMMENT '关联业务ID，供点击跳转',
  `is_read` tinyint(4) DEFAULT '0' COMMENT '0-未读 1-已读',
  `read_time` datetime DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_read` (`user_id`,`is_read`)
);

-- 25. 审核流程表（流程管理：清单要求"最好是活的"，按业务类型可配置）
CREATE TABLE `audit_flow` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `biz_type` tinyint(4) NOT NULL COMMENT '1-实名认证 2-商品上架 3-商品量价修改 4-订单审核 5-退款审核 6-贷款审核',
  `flow_name` varchar(50) NOT NULL,
  `enabled` tinyint(4) DEFAULT '1',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_biz_type` (`biz_type`)
);

-- 26. 审核节点表（每条流程可配置多级节点及审核角色，如订单=销售→主管）
CREATE TABLE `audit_node` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `flow_id` bigint(20) NOT NULL,
  `node_name` varchar(50) NOT NULL COMMENT '节点名称，如 销售审核/主管审核',
  `role_id` bigint(20) NOT NULL COMMENT '审核角色',
  `node_order` int(11) NOT NULL COMMENT '节点顺序，从1开始',
  PRIMARY KEY (`id`),
  KEY `idx_flow` (`flow_id`)
);

-- 27. 统一审核记录表（替代分散的审核日志：支撑"我的待办/我的已办"、两级审核流转与全程追溯）
CREATE TABLE `audit_record` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `biz_type` tinyint(4) NOT NULL COMMENT '1-实名认证 2-商品上架 3-商品量价修改 4-订单审核 5-退款审核 6-贷款审核',
  `biz_id` bigint(20) NOT NULL COMMENT '业务主键（用户ID/商品ID/订单ID/退款ID/贷款ID）',
  `biz_no` varchar(64) DEFAULT NULL COMMENT '业务单号（订单号/退款单号/贷款编号）',
  `biz_summary` varchar(255) DEFAULT NULL COMMENT '待办列表展示的摘要',
  `flow_node_id` bigint(20) DEFAULT NULL COMMENT '当前审核节点ID',
  `node_name` varchar(50) DEFAULT NULL COMMENT '当前节点名称快照',
  `status` tinyint(4) NOT NULL COMMENT '0-待审核 1-已通过 2-已驳回',
  `applicant_id` bigint(20) DEFAULT NULL COMMENT '申请人',
  `apply_time` datetime DEFAULT NULL,
  `auditor_id` bigint(20) DEFAULT NULL COMMENT '本节点审核人',
  `audit_time` datetime DEFAULT NULL,
  `remark` varchar(255) DEFAULT NULL COMMENT '审核意见',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_biz` (`biz_type`,`biz_id`),
  KEY `idx_status_node` (`status`,`flow_node_id`),
  KEY `idx_auditor` (`auditor_id`,`status`)
);

-- 28. 操作日志表（用户评级定级、额度调整等关键后台操作留痕，供追溯）
CREATE TABLE `sys_oper_log` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `operator_id` bigint(20) NOT NULL COMMENT '操作人',
  `module` varchar(50) DEFAULT NULL COMMENT '模块（用户管理/财务管理/流程管理等）',
  `action` varchar(100) DEFAULT NULL COMMENT '操作描述',
  `target_type` varchar(50) DEFAULT NULL COMMENT '目标类型',
  `target_id` varchar(64) DEFAULT NULL COMMENT '目标ID/业务单号',
  `detail` varchar(1000) DEFAULT NULL COMMENT '操作内容（JSON，含变更前后值）',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_operator` (`operator_id`),
  KEY `idx_target` (`target_type`,`target_id`)
);
```

### 3.1 初始化SQL

```sql
-- ============ 角色 ============
INSERT INTO `sys_role` (`id`,`role_name`,`description`) VALUES
(1,'超级管理员','系统全部权限'),
(2,'运营主管','商品/订单/退款终审，用户评级，财务'),
(3,'销售员','商品维护、订单销售审核、物流登记'),
(4,'客服','门户用户咨询支持、实名认证材料预检协助');

-- ============ 初始管理员 ============
-- 账号 admin / 初始密码 123456（下方为已验证的BCrypt哈希；如改用其他口令，
-- 可用 BCryptPasswordEncoder.encode("新口令") 重新生成后替换。上线前必须修改！）
INSERT INTO `sys_user` (`id`,`username`,`password`,`real_name`,`user_type`,`status`) VALUES
(1,'admin','$2b$10$9TEuQ4cWQTjJOAGN0OEJnOKZbv3wJePvPtPSXkoDh2BFqDgvTtaBC','系统管理员',3,1);
INSERT INTO `sys_user_role` (`user_id`,`role_id`) VALUES (1,1);

-- ============ 后台菜单（一级，二级/按钮权限按同样结构继续添加） ============
INSERT INTO `sys_permission` (`id`,`parent_id`,`name`,`type`,`perms`,`sort`) VALUES
(1,0,'首页看板',1,'admin:home',1),
(2,0,'任务管理',1,'admin:task',2),
(3,0,'商品管理',1,'admin:product',3),
(4,0,'订单管理',1,'admin:order',4),
(5,0,'用户管理',1,'admin:portal-user',5),
(6,0,'财务管理',1,'admin:finance',6),
(7,0,'资讯管理',1,'admin:article',7),
(8,0,'系统管理',1,'admin:system',8);
INSERT INTO `sys_role_permission` (`role_id`,`permission_id`)
SELECT 1, `id` FROM `sys_permission`;

-- ============ 商品分类（两级示例，三级继续挂 parent_id 即可） ============
INSERT INTO `product_category` (`id`,`parent_id`,`name`,`sort`) VALUES
(1,0,'肥料',1),(2,0,'农药',2),(3,0,'种子',3),(4,0,'农资用品',4),
(11,1,'氮肥',1),(12,1,'磷肥',2),(13,1,'钾肥',3),(14,1,'复合肥',4),(15,1,'有机肥',5),
(21,2,'杀虫剂',1),(22,2,'杀菌剂',2),(23,2,'除草剂',3),
(31,3,'粮食种子',1),(32,3,'蔬菜种子',2),
(41,4,'农膜',1),(42,4,'农机具',2);

-- ============ 审核流程与节点（可在后台"流程管理"中调整） ============
INSERT INTO `audit_flow` (`id`,`biz_type`,`flow_name`) VALUES
(1,1,'实名认证审核'),(2,2,'商品上架审核'),(3,3,'商品量价修改审核'),
(4,4,'订单审核'),(5,5,'退款审核'),(6,6,'贷款审核');
INSERT INTO `audit_node` (`flow_id`,`node_name`,`role_id`,`node_order`) VALUES
(1,'认证审核',2,1),
(2,'上架审核',2,1),
(3,'量价审核',2,1),
(4,'销售审核',3,1),
(4,'主管审核',2,2),
(5,'退款审核',2,1),
(6,'贷款审核',2,1);

-- ============ 消息模板 ============
INSERT INTO `message_config` (`code`,`title`,`content_template`) VALUES
('AUTH_RESULT','实名认证结果','您的实名认证已{结果}。{备注}'),
('PRODUCT_AUDIT_RESULT','商品审核结果','商品《{商品名}》审核{结果}。{备注}'),
('ORDER_AUDIT_RESULT','订单审核结果','订单{订单号}审核{结果}。{备注}'),
('ORDER_SHIPPED','订单发货通知','订单{订单号}已发货，物流公司：{物流公司}，单号：{物流单号}。'),
('REFUND_RESULT','退款处理结果','订单{订单号}退款申请已{结果}。{备注}'),
('LOAN_RESULT','贷款处理结果','您的贷款申请（{贷款编号}）已{结果}。{备注}'),
('TODO_REMIND','新的待办事项','您有新的{业务类型}待审核，请及时处理。'),
('SYSTEM_NOTICE','系统通知','{内容}');
```

---

## 四、开发顺序总览（里程碑）

| 阶段 | 主题 | 核心产出 | 先后顺序 | 参考工期 |
| :--- | :--- | :--- | :--- | :--- |
| **第1阶段** | 基础架构搭建 | 后端多模块骨架 + 前端脚手架 + 统一规范落地 | 并行搭建 | 2天 |
| **第2阶段** | 用户认证与权限 | 登录+权限+实名认证+用户评级+收货地址+改密 | **后台先行，H5跟进** | 3天 |
| **第3阶段** | 商品管理闭环 | 商品分类+商品管理+上架/量价审核闭环 | **后台先行，H5跟进** | 3天 |
| **第4阶段** | 订单交易闭环 | 购物车+下单支付+两级审核+发货签收+电子凭证 | **H5先行，后台跟进** | 5天 |
| **第5阶段** | 退款与评价 | 退款闭环（原路退回）+ 评价 | H5 + 后台并行 | 2天 |
| **第6阶段** | 财务管理 | 对账三件套+发票+贷款全流程+钱包 | **后台先行，H5跟进** | 3天 |
| **第7阶段** | 资讯管理 | 栏目+发布+我的发布+门户展示 | 后台先行，H5跟进 | 1.5天 |
| **第8阶段** | 看板与系统管理 | 统计看板+流程配置+消息中心 | 后台独立 | 2.5天 |
| **第9阶段** | 联调测试与收尾 | 全流程联调+测试+安全核查+部署 | 整体收尾 | 3天 |

> **合计约 25 人日**（按 1 后端 + 1 前端并行折算约 3 周）。此估算**不含**中行接口对接——银行侧工作量取决于对方提供接口文档的进度，建议单独立项。v1 估算的 14 天对 203 个功能点明显不足，请勿据此排期。

---

## 五、第1阶段：基础架构搭建（2天）

### 5.1 后端搭建（Spring Boot 3.2.x）

1. **创建父POM**（`backend/pom.xml`），模块：`agri-common`、`agri-service`、`agri-admin-api`、`agri-portal-api`。引入依赖：
   - Spring Boot Starter Web、Validation
   - MyBatis-Plus Starter（Boot3 专用 `mybatis-plus-spring-boot3-starter`）
   - MySQL Connector、Redis Starter (Lettuce)
   - JWT (jjwt 0.12.x)
   - Lombok、Hutool（工具）
   - Knife4j 4.x（OpenAPI3，接口调试）
   - Spring Boot Starter Test + Mockito（单元测试）
   - EasyExcel（对账凭据/报表导出）
2. **`agri-common`**：`Result<T>`、`PageResult<T>`、`ErrorCodeEnum`、`GlobalExceptionHandler`、`JwtUtils`、`RedisUtils`、`AesUtils`（敏感字段加解密）、`BaseEntity`(id/created_at/updated_at/deleted)、各状态枚举（与附录B对齐）。
3. **`agri-service`**：所有 Mapper 与 Service（用户、商品、订单、钱包、审核流、消息等）集中于此，两个 API 模块共同依赖，避免业务逻辑双写。
4. **配置 `application.yml`**（分 `dev` / `prod`，敏感值全部用环境变量）：

   ```yaml
   server:
     port: 8080
   spring:
     datasource:
       url: jdbc:mysql://${DB_HOST:localhost}:3306/agri_trading?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
       username: ${DB_USER:agri_app}
       password: ${DB_PASSWORD}
     data:
       redis:
         host: ${REDIS_HOST:localhost}
         port: 6379
     jackson:
       date-format: yyyy-MM-dd HH:mm:ss
       time-zone: GMT+8
   agri:
     jwt:
       secret: ${JWT_SECRET}
       expiration: 7200000 # 2小时
     aes:
       key: ${AGRI_AES_KEY}
     upload:
       path: ${UPLOAD_PATH:/data/agri/uploads}
   ```

   > 开发环境可在本机启动脚本里给环境变量临时赋值，**不要把真实值提交到代码库**。
5. **拦截器**：`agri-admin-api` 只拦截 `/api/admin/**`，`agri-portal-api` 只拦截 `/api/portal/**`（登录/注册除外）。校验流程：解析 JWT → **查 Redis 确认 token 存活**（退出/改密后删除，立即失效）→ userId 存入 ThreadLocal。
6. **定时任务骨架**：`@EnableScheduling` + `OrderTimeoutJob`（第4阶段启用）。

### 5.2 前端脚手架搭建

- **PC后台（frontend-admin）**：

  ```bash
  npm create vite@latest frontend-admin -- --template vue
  cd frontend-admin
  npm install element-plus axios vue-router pinia echarts
  npm run dev
  ```

  - 配置 `vue-router`：登录页 `/login`，主框架 `/`（侧边栏菜单由 `/api/admin/info` 返回的权限树动态渲染）。
  - Axios 拦截器：请求头加 `Authorization: Bearer token`；响应 `code=40100` 跳登录。
  - 封装 `request.js`（统一处理 Result、分页参数）与 `dict.js`（状态字典，与附录B一致）。

- **H5门户（frontend-portal）**：

  ```bash
  npm create vite@latest frontend-portal -- --template vue
  cd frontend-portal
  npm install vant@4 axios vue-router pinia
  npm install unplugin-vue-components -D   # Vant按需引入插件
  npm run dev
  ```

  - Vant 4 **按需引入**（`unplugin-vue-components` + `VantResolver`）。
  - 底部 Tabbar：首页 `/home`、分类 `/category`、购物车 `/cart`、我的 `/mine`。

**本阶段验收**：两端 `npm run dev` 可打开页面；后端两个服务启动后 `doc.html` 可访问；`/api/admin/login` 返回统一 Result 结构。

---

## 六、第2阶段：用户认证与权限体系（3天）

### 6.1 后台管理系统登录 + 权限 + 用户管理（**优先实现**）

**接口设计（agri-admin-api）**：

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 后台登录 | POST | `/api/admin/login` | 用户名密码 → 返回JWT + 用户信息 |
| 获取当前用户信息 | GET | `/api/admin/info` | 返回用户 + 角色 + 权限菜单树 |
| 修改密码 | PUT | `/api/admin/password` | 校验旧密码，成功后删除Redis中token强制重新登录 |
| 退出 | POST | `/api/admin/logout` | 删除Redis中的token（真正失效） |
| 角色管理 | CRUD | `/api/admin/role` | 增删改查 |
| 角色权限配置 | PUT | `/api/admin/role/{id}/permissions` | 勾选菜单/按钮权限 |
| 账户管理 | CRUD | `/api/admin/user` | **后台管理员**的增删改查、分配角色（即清单"分配账号密码和权限设置后登录"） |
| 商品分类管理 | CRUD | `/api/admin/category` | 树形多级分类维护（支持三级） |
| 门户用户列表 | GET | `/api/admin/portal-user/page` | 清单"用户管理"：用户名称、用户类型、联系人、联系电话、商家等级，支持按类型/等级筛选 |
| 门户用户详情 | GET | `/api/admin/portal-user/{id}` | 含实名认证资料（脱敏）、订单量、合作状况 |
| 用户评级定级 | PUT | `/api/admin/portal-user/{id}/credit` | 按下单量和合作状况设置 `credit_grade` 与 `credit_limit`（不同级别授信额度不同），写操作日志 |
| 我的待办 | GET | `/api/admin/task/pending` | 聚合 audit_record：实名认证、商品上架、量价修改、订单、退款、贷款，按当前登录人角色过滤节点 |
| 我的已办 | GET | `/api/admin/task/done` | 查 `audit_record` 中本人 `auditor_id` 且已审结的记录，支持查看详情 |
| 审核实名认证 | POST | `/api/admin/task/audit-auth` | 通过/驳回，写 audit_record，触发 AUTH_RESULT 消息 |

> **说明**：初始账号 `admin`（密码见初始化SQL，上线前必须修改）。JWT 存 Redis（key: `token:{userId}`，同账号互踢；如需多端登录，key 改为 `token:{userId}:{uuid}` 并调整清理策略）。

### 6.2 H5门户注册 + 实名认证 + 个人中心（**跟进开发**）

**接口设计（agri-portal-api）**：

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 门户注册 | POST | `/api/portal/register` | 手机号+密码+用户类型（自然人/法人），法人填企业名称；手机号幂等 |
| 门户登录 | POST | `/api/portal/login` | 返回JWT |
| 修改密码 | PUT | `/api/portal/password` | 同后台规则 |
| 基本信息查询 | GET | `/api/portal/profile` | 用户类型、手机号、银行卡信息（脱敏）、信用等级 |
| 基本信息修改 | PUT | `/api/portal/profile` | 联系人等非关键字段 |
| 提交实名认证 | POST | `/api/portal/auth/submit` | 真实姓名、身份证号、**营业执照号、营业执照图、身份证正反面、银行卡号、开户行、手机号**（清单要求含银行卡），写 audit_record（biz_type=1） |
| 查询认证状态 | GET | `/api/portal/auth/status` | 返回审核状态与驳回原因 |
| 收货地址列表 | GET | `/api/portal/address/list` | 支持多个 |
| 新增收货地址 | POST | `/api/portal/address` | 收货人、电话、省市区、详细地址、是否默认 |
| 修改收货地址 | PUT | `/api/portal/address/{id}` | |
| 删除收货地址 | DELETE | `/api/portal/address/{id}` | 逻辑删除 |
| 设为默认地址 | PUT | `/api/portal/address/{id}/default` | |
| 文件上传 | POST | `/api/portal/upload` | 门户端图片/凭证统一入口（认证材料、评价图片、盖章回传单），校验规则同后台 |
| 消息中心 | GET | `/api/portal/message/page` | 见第8阶段消息体系，可后置联调 |

**H5页面实现**（极简）：
- 注册页：手机号、密码、确认密码、用户类型（自然人/法人），法人额外企业名称。
- 实名认证页：表单（含银行卡号、开户行）+ 上传三证图片 → 提交后显示"审核中"。
- 我的页：认证状态标签、基本信息、**收货地址管理**、修改密码入口。

> **闭环验证**：H5提交认证 → 后台"我的待办"出现审核任务 → 审核通过 → H5状态变"已认证" → 后台"我的已办"可回看该审核详情；管理员可对用户评级并调整授信额度。

---

## 七、第3阶段：商品管理闭环（3天）

### 7.1 后台商品管理（**优先实现**）

**接口设计（agri-admin-api）**：

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 添加商品 | POST | `/api/admin/product` | 表单含分类、价格、库存、单位、图片 → status=0，写 audit_record（biz_type=2 商品上架） |
| 修改商品 | PUT | `/api/admin/product/{id}` | 修改基本信息，重新进入审核 |
| 修改价格/库存 | PUT | `/api/admin/product/{id}/price-stock` | 清单明确"发布后可修改数量及价格并重新提交审核"，走 biz_type=3（量价修改审核），与上架审核分开统计 |
| 删除商品 | DELETE | `/api/admin/product/{id}` | 逻辑删除（有进行中订单的商品禁止删除） |
| 分页查询商品 | GET | `/api/admin/product/page` | 条件：分类、状态、名称 |
| 审核商品 | POST | `/api/admin/task/audit-product` | 按 audit_record 的 biz_type（2上架/3量价）审核：通过→status=1，驳回→status=3，写审核记录+消息 |
| 上下架操作 | PUT | `/api/admin/product/{id}/shelf` | 已审核通过的商品：1上架/2下架（不再重复审核） |
| 推荐管理 | PUT | `/api/admin/product/{id}/recommend` | 0取消/1推荐，带排序值（展示位置管理） |
| 文件上传 | POST | `/api/admin/upload` | 图片/凭证统一上传入口，校验类型大小，返回URL |

**审核逻辑说明**：待办来自 `audit_record`（status=0 且节点角色=当前用户角色），不再直接扫 product 表；审核动作推进/终结 `audit_record`，并联动 `product.status`。

### 7.2 H5商品展示（**跟进开发**）

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 首页推荐商品 | GET | `/api/portal/product/recommend` | 推荐且已上架，按sort排序 |
| 商品分类树 | GET | `/api/portal/category/tree` | 多级分类 |
| 商品列表 | GET | `/api/portal/product/page` | 分类筛选、关键词搜索 |
| 商品详情 | GET | `/api/portal/product/{id}` | 详细信息 |

**H5页面**：
- 首页：顶部搜索框 → 分类导航图标 → 推荐商品列表（Vant `Grid` + `Cell`）。
- 分类页：左侧分类树（`Sidebar`），右侧商品列表（`List` 下拉加载）。
- 详情页：轮播图（`Swipe`）、价格、库存、规格 → 加入购物车（清单：下单数量须 ≤ 库存量，前端与后端双重校验）。

---

## 八、第4阶段：订单交易闭环（5天，**核心**）

### 8.1 H5购物车与下单（**H5先行**）

**接口设计（agri-portal-api）**：

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 添加购物车 | POST | `/api/portal/cart` | 商品id+数量，校验 ≤ 库存 |
| 修改购物车数量 | PUT | `/api/portal/cart` | |
| 删除购物车项 | DELETE | `/api/portal/cart/{productId}` | |
| 购物车列表 | GET | `/api/portal/cart/list` | 已选商品+总价 |
| 领取下单幂等Token | GET | `/api/portal/order/token` | 见统一规范 2.3 |
| 下单 | POST | `/api/portal/order/create` | 购物车选中项+`address_id`+幂等Token；商品名/价/分类快照入明细；状态=0待付款 |
| 支付 | POST | `/api/portal/order/pay` | `order_no` + `pay_method`：钱包→扣余额写流水；银行转账→返回平台结算账户信息/模拟支付成功（对接中行后走支付订单，见第十四章） |
| 我的订单列表 | GET | `/api/portal/order/page` | 状态Tab：**全部/待审核/待发货/待收货/已完成/退款**（清单五状态基础上因审核环节增加"待审核"，需与需求方确认） |
| 订单详情 | GET | `/api/portal/order/{id}` | 商品明细、收货信息、物流信息 |
| 取消订单 | PUT | `/api/portal/order/cancel` | 仅待付款可取消；**回补库存**，写取消时间 |
| 再次下单 | POST | `/api/portal/order/{id}/rebuy` | 将原订单商品重新加入购物车（校验在售与库存） |
| 查看发货单 | GET | `/api/portal/order/{id}/delivery` | 物流公司、单号 + 签章发货单附件（order_attachment type=2） |
| 电子凭证下载 | GET | `/api/portal/order/{id}/voucher` | 支付+审核通过后，服务端按订单数据生成 PDF 电子凭证（订单号/商品/金额/双方信息），支持下载打印 |
| 上传盖章回传单 | POST | `/api/portal/order/{id}/receipt` | 客户盖章回传（图片/PDF），写入 order_attachment type=1 |

**核心业务逻辑（下单 + 库存）**：
1. 校验幂等Token → 校验商品在售、数量 ≤ 库存、用户已实名认证。
2. 后端事务（`@Transactional`）：
   - `SELECT ... FOR UPDATE` 锁定商品行；
   - 扣减库存；
   - 生成订单号（雪花算法）；
   - 写订单主表（含收货信息快照、`address_id`）与明细（含分类快照）；
   - 清空已下单购物车项。
3. **库存回补**（v1缺失，必须实现）：取消订单、审核驳回取消、超时未付自动取消、退款成功四种场景，事务内 `stock = stock + quantity`。
4. **超时自动取消**：`OrderTimeoutJob` 每 1 分钟扫描"待付款且创建超过 30 分钟"的订单 → 取消 + 回补库存（阈值可配；也可用 Redis 延迟队列，初期定时任务足够）。
5. 支付成功后：`pay_status=1`、`pay_time=now()`，订单流转为 `1-待审核`，写 `audit_record`（biz_type=4，节点=销售审核），触发待办提醒消息。

**H5页面**：购物车（`Checkbox` 全选/单选、`Stepper`、底部结算栏）；确认订单页（**从地址簿选择/新增收货地址**、商品明细、合计、立即支付）；我的订单页（`van-tabs` 六状态，列表项含"再次下单""查看发货单""上传回传单""下载电子凭证"等操作按钮）。

### 8.2 后台订单审核与发货（**后台跟进**）

**接口设计（agri-admin-api）**：

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 订单列表 | GET | `/api/admin/order/page` | 全量订单，按状态、订单号、**商品分类**、时间筛选（清单备注：考虑按商品分类展示订单） |
| 审核订单 | POST | `/api/admin/task/audit-order` | **两级审核**（清单："由销售及主管审核"）：节点1 销售通过 → 流转节点2 主管；主管通过 → status 1→2（待发货）；任一节点驳回 → status 5（已取消）并**回补库存** |
| 信息登记（物流） | PUT | `/api/admin/order/delivery` | 销售登记物流公司、单号，可上传**加盖签章的发货单**（order_attachment type=2）→ status 2→3（待收货） |
| 订单确认完成 | PUT | `/api/admin/order/confirm` | 后台核验客户盖章回传单（order_attachment type=1）无误后确认 → status 3→4（已完成），触发评价入口 |
| 订单附件查看 | GET | `/api/admin/order/{id}/attachments` | 查看回传单/签章发货单 |

**页面实现**：待办列表展示待审核订单（含当前节点：销售审核/主管审核）；订单详情页展示支付金额、收货信息、附件；审核按钮按当前用户角色与节点显隐。

> **闭环验证**：下单支付 → 销售审核 → 主管审核 → 发货登记（附签章发货单）→ H5 上传盖章回传单 → 后台确认完成 → 下载电子凭证核对信息。

---

## 九、第5阶段：退款与评价（2天）

### 9.1 退款闭环（原路退回）

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 发起退款 | POST | `/api/portal/refund/apply` | orderId、退款金额、原因；仅已支付且状态为待审核/待发货/待收货可发起；同订单仅允许一笔进行中退款；订单状态→6退款中；写 audit_record（biz_type=5） |
| 退款详情 | GET | `/api/portal/refund/{orderId}` | 退款单状态 |
| 退款管理列表 | GET | `/api/admin/refund/page` | 清单要求字段：商品名称、交易金额、退款金额、退款原因、退款状态 |
| 退款审核 | POST | `/api/admin/refund/audit` | 通过→**原路退回**：`pay_method=钱包` → 退款入钱包并写流水（direction=1, trans_type=4）；`pay_method=银行转账` → 标记"待线下退款"，调用银行退款接口或财务线下完成后置为已退款；随后订单→7已退款并**回补库存**。驳回→订单恢复为 `refund_apply.prev_status`（发起退款时的状态快照） |

### 9.2 评价

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 提交评价 | POST | `/api/portal/comment/add` | 订单完成后：打分+文字+图片；同一订单同一商品仅一次 |
| 商品评价列表 | GET | `/api/portal/comment/page` | 按商品查 |
| 评价管理 | GET | `/api/admin/comment/page` | 清单要求：商品信息、买家信息、评价内容 |

---

## 十、第6阶段：财务管理（对账 + 发票 + 贷款 + 钱包）（3天）

### 10.1 后台财务管理

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 账户明细 | GET | `/api/admin/finance/account-detail` | 公司账户（平台结算账户）全部交易资金明细、收入支出详情；关联**财务凭证**字段（凭证影像上传/链接，清单备注"考虑财务凭证是否需要"——按需要实现，支持挂附件） |
| 收支账单 | GET | `/api/admin/finance/income-expense` | 按日/月汇总收入、支出 |
| 对账凭据 | GET | `/api/admin/finance/statement` | 查询+导出（EasyExcel 导出 Excel，PDF 后置） |
| 开票申请列表 | GET | `/api/admin/invoice/page` | 清单"对提交开票信息情况进行统计管理"：按状态/时间统计 |
| 开票 | POST | `/api/admin/invoice/{id}/issue` | 填写发票号码，置为已开票 |
| 驳回开票 | POST | `/api/admin/invoice/{id}/reject` | |
| 贷款列表 | GET | `/api/admin/loan/page` | 每一笔贷款信息、额度、金额、状态 |
| 贷款审核 | POST | `/api/admin/loan/{id}/audit` | 通过/驳回（驳回→状态3，释放占用额度） |
| 确认放款 | POST | `/api/admin/loan/{id}/release` | 银行放款后登记；**受托支付**：资金由银行直接支付至平台运营方结算账户，不进用户钱包，系统仅记录额度占用 |
| 确认还款 | POST | `/api/admin/loan/{id}/repay` | 银行还款后登记 → 状态2，**授信额度恢复** |

### 10.2 H5我的钱包 / 发票 / 贷款

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 钱包首页 | GET | `/api/portal/wallet/info` | 虚拟钱包：余额、冻结金额 |
| 资金流水 | GET | `/api/portal/wallet/transactions` | 分页返回流水号、金额、方向、交易后余额；关联订单的商品类型经 `order_id`→`order_detail` 取得（一单多品时按明细展开）；`trans_type` 筛选即得**出金/入金记录** |
| 入金（充值）申请 | POST | `/api/portal/wallet/deposit` | 初期模拟入账；对接中行后由入金回调驱动（第十四章） |
| 出金（提现）申请 | POST | `/api/portal/wallet/withdraw` | 初期模拟，出账至绑定银行卡 |
| 发票抬头列表 | GET | `/api/portal/invoice-info/list` | 支持多条 |
| 发票抬头增/改/删 | POST / PUT / DELETE | `/api/portal/invoice-info[/{id}]` | 清单"发票信息：添加、修改、删除" |
| 提交开票申请 | POST | `/api/portal/invoice/apply` | 选订单+发票抬头，金额 ≤ 订单实付 |
| 我的开票记录 | GET | `/api/portal/invoice/list` | 状态：待开票/已开票/驳回 |
| 贷款申请 | POST | `/api/portal/loan/apply` | 申请金额 ≤ 剩余授信额度，占用额度 |
| 我的贷款 | GET | `/api/portal/loan/info` | 当前授信额度、已用额度、剩余额度；**受托支付说明**：银行授信资金只能受托支付到平台运营方资金结算账户，不可挪作他用；额度随放款占用、随还款恢复；交易过程可查 |

---

## 十一、第7阶段：资讯管理（1.5天）

**后台**：
- 栏目管理 CRUD：`/api/admin/article-category`
- 发布文章：`POST /api/admin/article`（标题、富文本内容、栏目、发布人、是否发布）
- 所有文章：`GET /api/admin/article/page`（分页、编辑、删除）
- **我的发布**：`GET /api/admin/article/mine`（按 `author_id` 过滤当前登录人）

**H5**：
- 首页"行业资讯"列表：`GET /api/portal/article/list`（栏目筛选）
- 资讯详情：`GET /api/portal/article/{id}`（浏览量+1）
- 门户首页结构（清单）：导航栏、行业资讯、商品分类、推荐项目、个人中心入口。

---

## 十二、第8阶段：数据统计看板 + 系统管理（2.5天）

### 12.1 后台首页统计（`GET /api/admin/dashboard`）

按清单首页要求返回四类数据（前端用 ECharts 渲染）：
- **数据总览**：累计交易额、累计订单数、注册用户数、今日订单数/交易额、**待办数量**（聚合各业务类型待审核数，即"待办事项提醒"）；
- **年度项目交易统计**：当前年度按月交易金额/订单量柱状图；
- **交易价格统计**：重点商品成交均价与价格走势（近30日）；
- **交易商品类型分析**：按商品分类的销售金额占比（饼图，用 order_detail 分类快照统计）+ 近7日交易趋势。

### 12.2 流程管理（动态可配置，清单备注"最好是活的"）

| 接口 | 方法 | 路径 | 说明 |
| :--- | :--- | :--- | :--- |
| 流程列表 | GET | `/api/admin/flow` | 六类业务流程 + 各自节点（名称、审核角色、顺序） |
| 流程配置 | PUT | `/api/admin/flow/{bizType}` | 调整节点顺序、更换审核角色 |
| 新增节点 | POST | `/api/admin/flow-node` | 给某流程加审核节点 |
| 删除节点 | DELETE | `/api/admin/flow-node/{id}` | 至少保留一个节点 |

> 所有审核在运行时读取 `audit_flow/audit_node`，改配置即时生效，**不得写死枚举**。

### 12.3 消息配置与消息中心

- 消息配置 CRUD：`/api/admin/message-config`（模板、启停）。
- **触发机制**（v1缺失）：认证结果、商品/订单/退款/贷款审核结果、发货、新待办等事件发生时，按 `message_config` 模板渲染写入 `message_record`。
- 门户消息中心：`GET /api/portal/message/page`、`PUT /api/portal/message/{id}/read`、`PUT /api/portal/message/read-all`、`GET /api/portal/message/unread-count`（H5 Tabbar "我的"显示未读角标）。

---

## 十三、第9阶段：联调、测试与收尾（3天）

### 13.1 测试计划（清单含"测试"事项）

- **单元测试**（JUnit5 + Mockito，覆盖 Service 核心逻辑）：库存扣减/回补、订单状态机流转、两级审核流转、钱包余额变更、退款原路判定、额度占用/恢复。目标：核心 Service 覆盖率 ≥ 60%。
- **接口测试**：Knife4j / Postman 集合，全接口跑通；重点验证 401/403、参数校验、幂等（重复下单、重复支付回调）。
- **事务与异常**：库存不足、支付失败、审核并发（两个主管同时审同一单）场景验证回滚与乐观控制。
- **全流程回归清单**：注册→实名→评级→上架→下单支付→两级审核→发货→回传→完成→评价→退款→开票→贷款，逐条打勾。

### 13.2 安全核查清单

- [ ] JWT 过期返回 40100；退出/改密后旧 token 立即失效。
- [ ] 配置文件中无明文口令；生产口令走环境变量。
- [ ] 身份证/银行卡密文存储、接口返回脱敏。
- [ ] 上传文件白名单校验，目录不可执行。
- [ ] 越权检查：门户接口不能访问他人订单/钱包；后台按钮权限生效。

### 13.3 部署（简化）

- 后端：`mvn clean package` 打 jar，`java -jar *.jar`（admin 8080 / portal 8081），systemd 或容器托管。
- 前端：`npm run build` 生成 dist，Nginx 托管；`/uploads` 静态资源映射到上传目录。
- 跨域：生产由 Nginx 反向代理同域；开发环境用 Vite `proxy`，不依赖 `@CrossOrigin`。
- MySQL 每日备份；开启慢查询日志。

---

## 十四、专题：中行结算对接（清单"结算"条目）

**业务模型**（依据功能清单）：
- 平台在中行开立结算账户，统一收款；**不存在保证金，全部为货款**。
- 用户下单生成支付订单 → 货款缴纳 → 平台确认；订单取消**原路退款**。
- 贷款资金**受托支付**：银行授信资金只能支付到平台运营方资金结算账户，不可挪作他用。

**需与银行确认的接口清单**（拿到文档后单独立项）：
1. 生成支付订单（平台→银行：订单号、金额、附言）；
2. 到账通知回调（银行→平台：回调驱动订单支付成功，须做签名校验与幂等）；
3. 退款接口（原路退回）；
4. 账户余额/交易流水查询（对账用）。

**分期策略**：
- 一期（本手册范围）：`PayService` 抽象接口 + `MockPayServiceImpl`（模拟到账回调）；钱包入金/出金走模拟；`pay_method` 与 `refund_channel` 字段已按最终口径设计，切换时不动表结构。
- 二期：实现 `BocPayServiceImpl` 替换 Mock；新增日终对账任务（银行流水 vs `wallet_transaction`/`order_main`，差异清单人工处理）。

> 清单备注"如果是钱包模式，可能就几个接口：生成虚拟账号、入账通知、提现出账接口，退款缴款都走钱包？"——该模式选择需与银行最终确认，上述抽象保证两种模式均可落地。

---

## 十五、关键难点提示（避坑指南）

| 难点 | 解决方案 |
| :--- | :--- |
| **商品多级分类** | `parent_id` 递归；后端一次查全量（分类数据量小）程序内组装树，避免 N+1 查询 |
| **库存并发超卖** | 下单 `SELECT ... FOR UPDATE` 行锁；**取消/超时/退款必须回补库存**（v1漏项）；待付款超时30分钟自动取消 |
| **订单状态机** | `OrderStatusEnum` 定义合法流转，Service 层统一校验；禁止跨状态直改 |
| **两级审核与活流程** | 审核一律走 `audit_record` + `audit_flow/audit_node`，推进节点前重新读取配置；并发审核用乐观锁或唯一状态前置校验 |
| **幂等** | 下单用一次性Token；支付回调/退款用唯一单号；前端按钮防抖只是辅助，服务端校验为准 |
| **文件上传** | 统一 `/api/admin/upload`（门户单独提供 `/api/portal/upload`），白名单+改名；生产切 OSS |
| **电子凭证** | 用 iText / OpenPDF 按订单数据生成，注意中文字体打包进 jar |
| **支付对接** | 见第十四章：抽象 + Mock，字段口径提前对齐 |
| **敏感数据** | AES 入库 + 脱敏返回 + 日志脱敏（防止身份证打进日志） |
| **统计口径** | 一律基于 `order_detail` 的分类/金额快照统计，避免商品后续修改影响历史数据 |

---

## 十六、开发手册指令速查（每日开工用）

```bash
# 后端启动（先 common/service install，再启动两个API）
cd backend && mvn clean install -DskipTests
cd agri-admin-api && mvn spring-boot:run     # 8080
cd agri-portal-api && mvn spring-boot:run    # 8081

# PC前端启动
cd frontend-admin && npm run dev             # 3000

# H5前端启动
cd frontend-portal && npm run dev            # 3001
```

> **访问地址**：
> - PC后台：`http://localhost:3000`
> - H5门户：`http://localhost:3001`（F12 切换移动端视图调试）
> - 后台接口文档：`http://localhost:8080/doc.html`（Knife4j）
> - 门户接口文档：`http://localhost:8081/doc.html`

---

## 附录A：功能清单覆盖对照表

> 对照《农资现货交易系统功能清单》逐项核对（清单合计约203个功能点，下表按清单行聚合为主要功能项）。

**后台管理系统**：

| 清单项 | 覆盖位置 |
| :--- | :--- |
| 登录（分配账号密码和权限后登录） | 6.1 登录/账户管理/角色权限 |
| 首页：数据总览、年度项目交易统计、交易价格统计、交易商品类型分析、待办提醒 | 12.1 dashboard + ECharts |
| 任务管理-我的待办（实名/商品上架/订单确认/量价修改审核） | 6.1 `/task/pending` + audit_record |
| 任务管理-我的已办 | 6.1 `/task/done` |
| 商品管理：增删改查、上下架、展示位置、推荐、发布后改量价重新审核、多级分类 | 7.1 全部接口 |
| 订单管理-信息登记（物流+加盖签章） | 8.2 `/order/delivery` + order_attachment |
| 订单管理-订单确认（客户盖章回传后确认） | 8.1 `/receipt` + 8.2 `/order/confirm` |
| 订单管理-退款管理 | 9.1 `/admin/refund/page` |
| 订单管理-评价管理 | 9.2 `/admin/comment/page` |
| 用户管理（用户名称/类型/联系人/电话/商家等级） | 6.1 `/portal-user/page` |
| 用户评级定级（不同级别授信额度不同） | 6.1 `/portal-user/{id}/credit` |
| 财务管理-对账管理（账户明细/收支账单/对账凭据，含财务凭证） | 10.1 对账三接口 |
| 财务管理-发票管理 | 10.1 开票接口 |
| 财务管理-贷款服务 | 10.1 贷款接口 |
| 资讯管理：发布文章/我的发布/所有文章/栏目管理 | 十一章 |
| 系统管理-角色管理（增删改+权限配置） | 6.1 |
| 系统管理-账户管理（增删改） | 6.1 |
| 系统管理-消息提醒配置 | 12.3 |
| 系统管理-商品分类管理 | 6.1 `/category` |
| 系统管理-流程管理（审核流程/审核角色，动态配置） | 12.2 |

**门户网站**：

| 清单项 | 覆盖位置 |
| :--- | :--- |
| 注册登录（自然人/法人，评级定级影响授信） | 6.2 + 6.1 评级 |
| 实名认证（营业执照、身份证、手机号、银行卡，提交审核） | 6.2（含银行卡） |
| 首页（导航栏、行业资讯、商品分类、推荐项目、个人中心） | 7.2 + 十一章 |
| 行业资讯 | 十一章 |
| 商品分类查询、加购、下单（≤库存）、下单即支付、销售及主管审核、电子凭证下载打印 | 7.2、8.1、8.2（两级审核）、8.1 电子凭证 |
| 购物车下单支付 | 8.1 |
| 个人中心-基本信息（含修改密码） | 6.2 profile/password |
| 个人中心-发票信息（增改删） | 10.2 |
| 个人中心-收货地址（多个） | 6.2 |
| 我的订单：全部/待发货/待收货/已完成/退款，再次下单、查看发货单 | 8.1（六Tab+rebuy+delivery） |
| 我的钱包-我的账户/资金流水/出金入金记录 | 10.2 |
| 我的钱包-我的贷款（授信额度、受托支付、额度占用与恢复） | 10.2 + 6.1 评级 |
| 结算（中行接口、货款直付、取消原路退款） | 十四章 + 8.1/9.1 |
| 测试 | 13.1 |
| 部署 | 13.3 |

> 清单中待确认项的处理：① "商品下单前是否需要客服沟通"——清单标注"暂时不用"，本手册不实现；② "财务凭证是否需要"——按需要实现附件挂载（10.1）；③ 待办中的"订单确认审核"并入订单审核流程（biz_type=4），盖章回传后的"订单确认"为后台操作（8.2）不入待办，如需待办化请与需求方确认；④ 流程管理的"新增/删除"子项：六类业务流程内置固定，仅支持节点级增删改（12.2），不做流程级增删；⑤ 无结论项（首页消息提醒、按商品分类展示订单）均已按"实现"处理。

---

## 附录B：状态字典汇总

| 字典 | 取值 |
| :--- | :--- |
| order_status | 0-待付款 1-待审核 2-待发货 3-待收货 4-已完成 5-已取消 6-退款中 7-已退款 |
| pay_status | 0-未支付 1-已支付 |
| pay_method | 钱包 / 银行转账 / 额度（贷款受托支付，预留） |
| refund_channel | 钱包 / 银行转账（原路退回） |
| product.status | 0-待审核 1-已上架 2-已下架 3-审核驳回 |
| auth_status | 0-未认证 1-审核中 2-已认证 3-驳回 |
| refund_apply.status | 0-待审核 1-同意 2-驳回 3-已退款 |
| loan_record.status | 0-申请中 1-已放款 2-已还款 3-驳回 |
| invoice_apply.status | 0-待开票 1-已开票 2-驳回 |
| audit_record.status | 0-待审核 1-已通过 2-已驳回 |
| audit_record.biz_type | 1-实名认证 2-商品上架 3-商品量价修改 4-订单审核 5-退款审核 6-贷款审核 |
| wallet_transaction.trans_type | 1-入金 2-出金 3-支付 4-退款 |
| wallet_transaction.direction | 1-入账 2-出账 |
| order_attachment.type | 1-客户盖章回传单 2-签章发货单 |
| wallet_transaction.trans_status | 0-处理中 1-成功 2-失败 |
| message_record.is_read | 0-未读 1-已读 |
| sys_user.status | 0-禁用 1-启用 |
| product_category.status | 0-停用 1-启用 |
| article.is_published | 0-草稿 1-已发布 |

> 任何状态新增/调整，必须同步修改：建表注释、`agri-common` 枚举、前端 `dict.js`、本附录。
